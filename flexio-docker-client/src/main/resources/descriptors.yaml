containerCreationLog:
  container:
    $value-object: io.flexio.docker.api.types.Container
  success: bool
  action:
    $enum: CREATION, NONE, UPDATE, PULL
  message: string
containerStartLog:
  container:
    $value-object: io.flexio.docker.api.types.Container
  success: bool
  action:
    $enum: START, NONE
  message: string
containerDeletionLog:
  container:
    $value-object: io.flexio.docker.api.types.Container
  success: bool
  action:
    $enum: DELETE, NONE
  message: string
containerStopLog:
  container:
    $value-object: io.flexio.docker.api.types.Container
  success: bool
  action:
    $enum: STOP, NONE
  message: string